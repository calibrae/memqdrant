name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  check:
    name: check / ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust (stable)
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: fmt
        run: cargo fmt --all -- --check

      - name: clippy (fastembed, default features)
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: clippy (ollama)
        run: cargo clippy --all-targets --locked --no-default-features --features ollama -- -D warnings

      - name: build (fastembed)
        run: cargo build --release --locked

      - name: build (ollama)
        run: cargo build --release --locked --no-default-features --features ollama

      - name: test
        run: cargo test --locked

  audit:
    name: audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install cargo-audit
        run: cargo install --locked cargo-audit
      - name: Audit
        # Ignored advisories (all informational "unmaintained", no security impact):
        #   RUSTSEC-2024-0436 — paste 1.0.15, transitive via ort → fastembed.
        run: cargo audit --deny warnings --ignore RUSTSEC-2024-0436
