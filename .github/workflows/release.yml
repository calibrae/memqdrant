name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: ${{ matrix.flavour }}-${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Ollama flavour (default) — one artifact per target.
          - flavour: ollama
            target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - flavour: ollama
            target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            cross: true
          - flavour: ollama
            target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            cross: true
          - flavour: ollama
            target: aarch64-apple-darwin
            os: macos-14
          # Fastembed flavour — only the targets that sensibly host ONNX runtime.
          # Linux musl is intentionally skipped (ort-sys prebuilts are glibc only).
          - flavour: fastembed
            target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - flavour: fastembed
            target: aarch64-apple-darwin
            os: macos-14
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.flavour }}-${{ matrix.target }}

      - name: Install cross (Linux cross-builds)
        if: matrix.cross
        run: cargo install --locked cross

      - name: Cargo build flags
        id: flags
        shell: bash
        run: |
          if [ "${{ matrix.flavour }}" = "fastembed" ]; then
            echo "args=--no-default-features --features fastembed" >> "$GITHUB_OUTPUT"
            echo "suffix=-fastembed" >> "$GITHUB_OUTPUT"
          else
            echo "args=" >> "$GITHUB_OUTPUT"
            echo "suffix=" >> "$GITHUB_OUTPUT"
          fi

      - name: Build
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --locked --target ${{ matrix.target }} ${{ steps.flags.outputs.args }}
          else
            cargo build --release --locked --target ${{ matrix.target }} ${{ steps.flags.outputs.args }}
          fi

      - name: Package
        id: pkg
        shell: bash
        run: |
          TAG="${GITHUB_REF_NAME:-${{ github.event.inputs.tag }}}"
          NAME="memqdrant${{ steps.flags.outputs.suffix }}-${TAG}-${{ matrix.target }}"
          mkdir -p "dist/${NAME}"
          cp "target/${{ matrix.target }}/release/memqdrant" "dist/${NAME}/"
          cp README.md LICENSE "dist/${NAME}/"
          cd dist
          tar czf "${NAME}.tar.gz" "${NAME}"
          shasum -a 256 "${NAME}.tar.gz" > "${NAME}.tar.gz.sha256"
          echo "archive=dist/${NAME}.tar.gz" >> "$GITHUB_OUTPUT"
          echo "sha256=dist/${NAME}.tar.gz.sha256" >> "$GITHUB_OUTPUT"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: memqdrant${{ steps.flags.outputs.suffix }}-${{ matrix.target }}
          path: |
            ${{ steps.pkg.outputs.archive }}
            ${{ steps.pkg.outputs.sha256 }}
          if-no-files-found: error

  release:
    name: release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name || github.event.inputs.tag }}
          name: ${{ github.ref_name || github.event.inputs.tag }}
          generate_release_notes: true
          files: |
            artifacts/*.tar.gz
            artifacts/*.sha256
